/**
 * 装备详情
 */
import React,{
    Component,
    StyleSheet,
    View
} from 'react-native';
let styles = StyleSheet.create({})

export default class EquipDetail extends Component {
    render() {
        return (
            <View></View>
        )
    }
}

EquipDetail.propTypes = {}
