/**
 * 分享选择对话框
 */
'use strict'
import React,{
    Component,
    View,
    StyleSheet
} from 'react-native';
let styles = StyleSheet.create({})

export default class ShareDialog extends Component {
    render() {
        return (
            <View></View>
        )
    }
}

ShareDialog.propTypes = {}
