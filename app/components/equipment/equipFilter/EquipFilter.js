/**
 * 装备筛选栏
 */
'use strict';
import React,{
    Component,
    View,
    Text,
    StyleSheet
} from 'react-native';
let styles = StyleSheet.create({})
export default class EquipFilter extends Component {
    render() {
        return (
            <View></View>
        )
    }
}

EquipFilter.propTypes = {};
