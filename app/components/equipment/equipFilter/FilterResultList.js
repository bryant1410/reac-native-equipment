/**
 * 筛选结果面板
 */
'use strict'
import React,{
    Component,
    StyleSheet,
    View
} from 'react-native';

let styles = StyleSheet.create({});

export default class FilterResultList extends Component {
    render() {
        return (
            <View></View>
        )
    }
}
FilterResultList.propTypes = {}
