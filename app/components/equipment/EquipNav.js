/**
 * 装备库首页导航栏
 */
'use strict'
import React,{
    Component,
    StyleSheet,
    View
} from 'react-native';
let styles = StyleSheet.create({})

export default class EquipNav extends Component {
    render() {
        return (
            <View></View>
        )
    }
}

EquipNav.propTypes = {}
