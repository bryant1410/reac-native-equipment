/**
 * 装备参数容器
 */
'use strict';
import React,{
    Component,
    View,
    StyleSheet
} from 'react-native';

let styles = StyleSheet.create({});
export default class ParamsContainer extends Component {
    render() {
        return (
            <View></View>
        )
    }
}

ParamsContainer.propTypes = {}
