/**
 * 装备的基础信息
 * 包括 装备名称，参考价
 */
import React,{
    Component,
    StyleSheet,
    View
} from 'react-native';

let styles = StyleSheet.create({})

export default class BaseInfo extends Component {
    render() {
        return (
            <View></View>
        )
    }
}
BaseInfo.propTypes = {}
