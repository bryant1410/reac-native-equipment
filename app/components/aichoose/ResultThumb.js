/**
 * 选装结果缩略图
 * 用于展示上一次选装结果
 */
import React,{
    Component,
    View,
    StyleSheet,
    Image,
    Text
} from 'react-native';
let styles = StyleSheet.create({})
export default class ResultThumb extends Component {
    render() {
        return (
            <View></View>
        )
    }
}

ResultThumb.propTypes = {}
