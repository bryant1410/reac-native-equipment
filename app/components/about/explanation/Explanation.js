/**
 * 应用说明
 */
'use strict'
import React,{
    Component,
    Text,
    View,
    StyleSheet
} from 'react-native';
let styles = StyleSheet.create({})

export default class Explanation extends Component {
    render() {
        return (
            <View></View>
        )
    }
}
Explanation.propTypes = {}
