/**
 * 装备库的reducers
 */
'use strict';
import createReducer from '../utils/create-reducer';
import {EQUIPMENT} from '../config/actionType';

const initialState = {}
const actionHandle = {}

export default createReducer(initialState, actionHandle);
